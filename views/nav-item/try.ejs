<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Trips Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    .lead-card.lead-row {
      display: flex;
      justify-content: space-between;
      padding: 15px 20px;
      border: 1px solid #ddd;
      border-radius: 8px;
      margin-bottom: 15px;
      align-items: center;
      cursor: pointer;
      transition: background-color 0.2s;
    }
    .lead-card.lead-row:hover {
      background-color: #f8f9fa;
    }
    .lead-info {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 600;
      font-size: 1rem;
    }
    .lead-meta {
      display: flex;
      align-items: center;
      gap: 15px;
      font-size: 0.9rem;
      color: #6c757d;
    }
    .lead-meta .fw-medium {
      color: #212529;
      font-weight: 500;
    }
  </style>
</head>
<body class="container my-4">
  <h1>All Trips</h1>

  <% if (detailedTrips.length === 0) { %>
    <p>No trips found.</p>
  <% } else { %>
    <% detailedTrips.forEach(({ trip, goingStops, timing }) => { %>
      <div class="lead-card lead-row">
        <div class="lead-info">
          <i class="text-primary bi bi-hourglass-split"></i>
          <span class="fw-semibold"><%= trip.pickupLocation %></span>
          <span class="fw-semibold">
            <span class="material-icons text-primary" style="font-size:20px; margin-right:8px; vertical-align:middle;">
              arrow_forward
            </span>
            <%= trip.destinationLocation %>
          </span>
        </div>
        <div class="lead-meta">
          <span class="text-muted"><%= timing?.departureDate || 'N/A' %></span>
          <span class="fw-medium">€300</span> <!-- Price hardcoded, change as needed -->
          <span class="d-flex align-items-center gap-1">
            <span><%= trip.numberOfPeople %></span>
            <span class="material-icons">groups</span>
          </span>
          <span class="material-icons text-primary">chevron_right</span>
        </div>
      </div>
    <% }) %>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
